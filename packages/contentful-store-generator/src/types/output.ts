export enum Namespace {
    Content = 'Content',
    Field = 'Field',
    Link = 'Link'
}

export enum Content {
    Asset = 'Asset',
    Entry = 'Entry',
}

export enum Field {
    Location = 'Location',
    JSON = 'JSON',
}
